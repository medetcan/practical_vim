* In visual and operator pending mode "i" and "a" commands behaves differently they form the first half of a text object
    * "i" means in visual and operator pending mode inner text object
    * "a" means in visual and operator pending mode a text object
